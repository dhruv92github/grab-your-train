import React from 'react';

const Header=(props)=>{

return(<div className="App">
        <header className="App-header">
          {/* <img src={logo} className="App-logo" alt="logo" /> */} 
          <h1 className="App-title">Grab your Train</h1>
        </header>
      </div>
	)
	
}
export default Header;